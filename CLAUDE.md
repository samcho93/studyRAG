# CLAUDE.md — RAG Lab 강의 사이트

이 파일은 Claude Code가 이 저장소에서 작업할 때 따라야 할 프로젝트 규칙입니다.

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 정식 교과목명 | 검색증강생성(RAG) 시스템 구축 |
| 부제 | 문서 파싱부터 평가까지, 동작하는 RAG 만들기 |
| 사이트 명 | **RAG Lab** |
| 배포 URL | `https://samcho93.github.io/studyRAG` |
| 대상 | 한국폴리텍대학 분당융합기술교육원 AI응용소프트웨어과 학생 |
| 기간 | 12주 (주 1회 3시간 기준) |
| 언어 | 한국어 (코드 주석·변수명은 영어) |

### 이 사이트가 다른 RAG 강의와 다른 점

1. **브라우저에서 직접 실습**한다 — 설치 없이 페이지에서 바로 임베딩·검색·생성을 돌려본다
2. **평가(Evaluation)까지 간다** — 튜토리얼에서 끝내지 않고 Recall@k, Faithfulness를 측정한다
3. **고장난 RAG를 고친다** — 일부러 망가뜨린 예제를 주고 증상→원인을 추적하게 한다

---

## 2. 기술 스택과 절대 제약

### 스택

- **순수 HTML + CSS + Vanilla JavaScript** (ES Modules)
- 빌드 도구 **없음**, 번들러 **없음**, 프레임워크 **없음**
- GitHub Pages 정적 호스팅

### 절대 제약 (위반 시 사이트가 동작하지 않음)

| 제약 | 이유 |
|---|---|
| Node 빌드 과정을 도입하지 말 것 | `git push` 만으로 배포되는 구조를 유지한다 |
| 서버 코드(Express, FastAPI 등)를 만들지 말 것 | 정적 호스팅이라 실행되지 않는다 |
| API 키를 코드나 저장소에 넣지 말 것 | 공개 저장소다. 키는 사용자가 입력한다 |
| `http://` 리소스를 참조하지 말 것 | HTTPS 페이지에서 mixed content로 차단된다 |
| CDN은 `https://` + SRI 해시 또는 버전 고정 | 재현성 확보 |

### 외부 의존성 (CDN, 버전 고정 필수)

```html
<!-- 브라우저 내 임베딩 -->
<script type="module">
  import { pipeline } from 'https://cdn.jsdelivr.net/npm/@huggingface/transformers@3.x.x';
</script>
```

- `@huggingface/transformers` (Transformers.js) — 브라우저 내 임베딩 추론
- 그 외 라이브러리는 **추가하기 전에 반드시 사용자에게 확인**받을 것

---

## 3. 디렉토리 구조

```
studyRAG/
├── index.html                  # 랜딩: 과정 소개 + 주차 목차 + 학생용/교수자용 진입
├── student.html / teacher.html # 학생용·교사용 진입점 (리다이렉트)
├── assets/
│   ├── css/
│   │   ├── tokens.css          # 디자인 토큰 (색·타이포·간격) — 여기만 수정해 테마 변경
│   │   ├── base.css            # 리셋 + 기본 타이포그래피
│   │   ├── components.css      # 3단 셸, 카드, 코드 실행기, 실습위젯 공통 스타일
│   │   └── slides.css          # 교사용 PPT 슬라이드(16:9) + 교사 노트
│   ├── js/
│   │   ├── core/
│   │   │   ├── embed.js        # Transformers.js 래퍼 (모델 로딩·캐시·임베딩)
│   │   │   ├── bm25.js         # BM25 순수 JS 구현
│   │   │   ├── vectorstore.js  # 코사인 유사도 기반 인메모리 벡터스토어
│   │   │   ├── chunker.js      # 고정/오버랩/문장/재귀 청킹
│   │   │   ├── rerank.js       # RRF 융합 + 스코어 기반 리랭킹
│   │   │   ├── llm.js          # 사용자 API 키로 LLM 호출 (공급자 추상화)
│   │   │   └── metrics.js      # Recall@k, MRR, nDCG 계산
│   │   ├── widgets/            # 주차별 실습 위젯 (아래 6절 규칙 준수)
│   │   └── site/               # site.js(목차·진도·테마) · weeks.js(주차 메타) · slides.js(슬라이드)
│   │                           # result.js(오른쪽 실습 결과 창) · runner.js(코드 ▶ 실행, Web Worker)
│   └── data/
│       ├── corpus/             # 전 과정 공통 문서셋 (JSON)
│       └── golden/             # 평가용 골든셋 (질문-정답-근거)
├── weeks/
│   ├── w01/
│   │   ├── index.html          # 학생용 (문서 + 실습)
│   │   ├── teacher.html        # 교사용 (PPT 슬라이드 + 교사 노트 + 부록)
│   │   └── lab.html            # 실습 위젯 페이지 (선택)
│   ├── w02/ ... w12/
├── theory/                     # 주차에 종속되지 않는 개념 설명 페이지
├── teacher/
│   └── index.html              # 교수자용 허브 (강의안·정답·루브릭)
└── CLAUDE.md
```

---

## 4. 브라우저 내 실습 아키텍처 (이 프로젝트의 핵심)

정적 사이트에서 RAG 전체 파이프라인을 돌리기 위한 설계다. **이 구조를 깨는 제안을 하지 말 것.**

```
┌─────────────────── 브라우저 안에서 전부 실행 ───────────────────┐
│                                                                │
│  문서(JSON)  →  청킹(chunker.js)  →  임베딩(Transformers.js)    │
│                                          ↓                     │
│                                   인메모리 벡터스토어           │
│                                          ↓                     │
│  질의 → [BM25 + 벡터] 하이브리드 → RRF 융합 → 리랭킹 → 컨텍스트 │
│                                                        ↓       │
└────────────────────────────────────────────────────────┼───────┘
                                                         ↓
                              (선택) 사용자 API 키로 LLM 호출 → 답변 + 인용
```

### 4-1. 임베딩 — Transformers.js

- 기본 모델: 경량 다국어 모델 (초기 로딩 30~60MB)
- **최초 1회만 다운로드**되고 이후 브라우저 캐시에 저장됨 → 로딩 진행률 UI 필수
- 한국어 성능 비교 실습용으로 모델 선택 드롭다운 제공
- WebGPU 사용 가능 시 자동 활용, 불가 시 WASM 폴백

```js
// assets/js/core/embed.js 사용 예
import { embed, loadModel } from '../core/embed.js';
await loadModel('Xenova/multilingual-e5-small', onProgress);
const vec = await embed('검색증강생성이란 무엇인가');
```

### 4-2. LLM 호출 — 사용자 API 키 방식

- 키는 **`sessionStorage`에만** 저장 (탭 닫으면 소멸). `localStorage` 금지
- 키 입력란은 `type="password"`, 화면·콘솔·에러 메시지에 절대 노출 금지
- 공급자별 CORS 요건을 `llm.js`에서 흡수한다
- **LLM 없이도 1~9주차 실습이 전부 동작해야 한다** (검색까지는 키 불필요)

### 4-3. 실습 데이터

- 전 과정을 **동일 문서셋 하나**로 관통한다 → 학생이 정답을 아는 문서를 쓴다
- 기본 코퍼스: 학과 교육과정 안내 + RAG 이론 교안 자체 (자기참조 구조)
- 스캔 PDF·표 깨짐 등 **의도적으로 망가진 문서를 포함**시킨다 (3주차 실습 재료)

---

## 5. 12주 커리큘럼 매핑

| 주차 | 주제 | 실습 위젯 | LLM 키 |
|---|---|---|---|
| 01 | LLM 기초 & RAG 필요성 | 토큰 카운터, 환각 체험 | 선택 |
| 02 | 프롬프트 엔지니어링 | 프롬프트 A/B 비교 | 필요 |
| 03 | 문서 로딩 & 파싱 | PDF 텍스트 추출 뷰어 | — |
| 04 | 청킹 전략 | **청킹 플레이그라운드** | — |
| 05 | 임베딩 & 유사도 | **유사도 히트맵 + 2D 투영** | — |
| 06 | 벡터DB & 인덱스 | Flat vs HNSW 속도 비교 | — |
| 07 | 최소 RAG 완성 | 전체 파이프라인 실행 | 필요 |
| 08 | 하이브리드 검색 + 리랭킹 | **BM25 vs 벡터 vs 하이브리드** | — |
| 09 | 평가 데이터셋 & 측정 | **점수판(Recall@k, nDCG)** | 선택 |
| 10 | 프레임워크 리팩터링 | 코드 비교 뷰어 | — |
| 11 | 웹 UI + 인용 표기 | 인용 하이라이트 데모 | 필요 |
| 12 | 팀 프로젝트 발표 | 팀별 결과 업로드 | — |

**굵게 표시된 위젯이 이 사이트의 핵심 자산**이다. 우선순위를 높게 둘 것.

---

## 6. 실습 위젯 작성 규칙

모든 위젯은 다음을 지킨다.

1. **하나의 개념만** 다룬다. 여러 개를 합치지 않는다
2. **입력을 바꾸면 결과가 즉시 바뀐다** — 설명보다 조작이 먼저다
3. **실패를 보여준다** — "이 설정에서는 왜 검색이 실패하는가"를 볼 수 있어야 한다
4. `<template>` + ES Module 조합으로 작성하고, 위젯 하나 = 파일 하나
5. 초기 상태에서 이미 뭔가 보여야 한다. 빈 화면 + "시작하기" 버튼 금지
6. 모바일 세로 화면(375px)에서 조작 가능해야 한다

```js
// widgets/w04-chunking.js 기본 형태
export function mount(el, options = {}) { /* ... */ }
export function unmount(el) { /* 리스너·모델 정리 */ }
```

---

## 7. 콘텐츠 작성 규칙

### 학생용 (`weeks/wNN/index.html`)

고정된 6개 섹션 순서를 지킨다.

1. **학습 목표** — "~할 수 있다" 3개 이내
2. **왜 필요한가** — 이 기법이 없을 때 생기는 문제부터 제시
3. **개념** — 그림/도식 우선, 수식은 직관 설명 뒤에
4. **실습** — 위젯 조작 → 관찰 질문
5. **Challenge** — 4~5개, 난이도 오름차순, 코드 스켈레톤 제공
6. **정리 & 다음 주 예고**

### 교수자용 (`weeks/wNN/teacher.html`) — PPT형 슬라이드 덱

- 16:9 슬라이드(`<section class="slide">`) + 슬라이드마다 교사 노트(`<aside class="notes">`)
- 단축키: `←` `→` 이동 · `F` 전체 화면 · `N` 노트 · `B` 가리기 · `T` 타이머 · `P` 발표자 창
- 데모 슬라이드(`slide--demo`)에 실제 위젯을 넣고, 코드 슬라이드(`pre[data-run]`)는 ▶ 실행 가능
- 아래 1~5는 덱 끝의 **부록 슬라이드**(`data-appendix`)로 넣는다

1. 주차 강의안 (시간 배분 포함)
2. Challenge 정답 코드 + 왜 그렇게 푸는지
3. **학생이 자주 막히는 지점**과 대응
4. 토론 주제
5. 평가 루브릭

### 문체와 용어

- 학생용은 평서체(`~한다`)로 통일. 존댓말 혼용 금지
- 영문 용어는 **첫 등장 시 1회만 병기**: 검색증강생성(RAG) → 이후 RAG
- 표기 통일: `임베딩`, `청킹`, `리랭킹`, `벡터스토어`, `하이브리드 검색`
- 코드 블록은 언어 명시(` ```python `, ` ```javascript `)

---

## 8. 디자인 규칙

- **3단 화면** (학생용·교사용 공통): 왼쪽 강의 목록(`.shell-toc`) · 가운데 강의 내용(`.shell-main`) · 오른쪽 실습 결과(`.shell-result`)
  - 위젯 출력과 코드 실행 결과는 `result.js`의 `registerOutput()`으로 오른쪽 창에 보낸다
  - 1000px 미만에서는 목록이 서랍으로 바뀌고 결과는 원래 자리(인라인)로 돌아온다

- 색·간격·폰트는 **`tokens.css`의 CSS 변수만** 사용한다. 하드코딩 금지
- 다크모드 필수: `@media (prefers-color-scheme: dark)` + `[data-theme]` 수동 토글 둘 다 지원
- 모바일 반응형 필수 (학생이 폰으로 본다). 최소 375px, 좌우 여백 16px
- 본문 최대 폭 `72ch`
- 접근성: 대비비 4.5:1 이상, 모든 인터랙티브 요소에 키보드 포커스 표시
- 아이콘은 인라인 SVG. 아이콘 폰트 라이브러리 도입 금지

---

## 9. 반드시 지킬 것 / 하지 말 것

### 반드시

- [ ] 새 페이지를 만들면 `index.html` 목차와 이전/다음 네비게이션을 함께 갱신
- [ ] 위젯 추가 시 로딩 상태·에러 상태 UI를 함께 구현
- [ ] `iframe`으로 실습 페이지를 넣을 경우 부모에 `allow` 속성 확인
- [ ] 외부 CDN 버전은 고정. `@latest` 사용 금지
- [ ] 큰 모델 파일은 저장소에 커밋하지 않고 CDN에서 로드

### 하지 말 것

- [ ] 빌드 스텝·번들러·프레임워크 도입
- [ ] API 키 하드코딩, 로그 출력, `localStorage` 저장
- [ ] 서버가 필요한 기능 제안 (필요하면 먼저 사용자에게 알리고 대안 제시)
- [ ] 한 번에 여러 주차를 동시에 작성 — 한 주차씩 완성하고 검토받는다
- [ ] 기존 파일을 통째로 재작성 — 부분 수정을 우선한다

---

## 10. 작업 방식

- 새 주차 작업 시: **학생용 → 위젯 → 교수자용** 순서로 만든다
- 코드 변경 후 `python3 -m http.server`로 로컬 확인 절차를 안내한다
  (ES Module과 fetch는 `file://`에서 동작하지 않는다)
- 커밋 메시지: `feat(w04): 청킹 플레이그라운드 위젯 추가` 형식
- 큰 구조 변경은 착수 전에 계획을 먼저 제시하고 승인받는다

---

## 11. 진행 상태

### Phase 1 — 기반
- [x] `tokens.css` / `base.css` / `components.css`
- [x] `index.html` 랜딩 + 주차 목차
- [x] 3단 화면(목록·내용·실습 결과) + 교사용 PPT 슬라이드 엔진 + 코드 실행기
- [x] `core/embed.js` (Transformers.js 래퍼 + 로딩 UI) — 브라우저 실측 검증은 5주차 위젯에서
- [x] `core/` bm25 · vectorstore · chunker · rerank · metrics · llm
- [x] 공통 코퍼스 `assets/data/corpus/` + 골든셋 `assets/data/golden/`

### Phase 2 — 핵심 위젯
- [x] w04 청킹 플레이그라운드
- [ ] w05 유사도 히트맵
- [ ] w08 BM25 vs 벡터 vs 하이브리드 비교
- [ ] w09 평가 점수판

### Phase 3 — 교안
- [ ] w01~w12 학생용 (w04 완료)
- [ ] w01~w12 교수자용 (w04 완료)

### Phase 4 — 마감
- [ ] 모바일 점검 / 다크모드 점검 / 접근성 점검
- [ ] GitHub Pages 배포 및 캐시 확인

---

## 12. 참고

- 기존 프로젝트와 톤·구조를 맞춘다면 해당 저장소 경로를 알려주면 스타일을 정렬한다
- 한국어 임베딩 모델 선택 근거는 `theory/embedding-models.html`에 정리한다
  (KURE-v1/v2, BGE-M3, OpenAI text-embedding-3 비교)
